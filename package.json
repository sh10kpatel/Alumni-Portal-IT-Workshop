{
  "name": "alumni",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "scripts": {
    "start": "cd server && npm install && npm start",
    "dev": "cd server && npm run dev"
  }
}
